# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
.fseventsd
.DocumentRevisions-V100
.TemporaryItems
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Local/editor state
.env
.env.*
!.env.example
.vscode/
.idea/

# Build/cache outputs
dist/
build/
target/
node_modules/
.cache/
coverage/

# Python virtualenv (uv-managed; uv.lock is committed, the venv is not)
.venv/

# Tool caches
.ruff_cache/

# Live PEP sandbox: its own nested git repo (probe-verified hook isolation, ADR-0021).
# A disposable harness for wiring the `enforce` hook against real Claude/Codex sessions;
# the durable wiring snippet lives in the provider-hooks skill, not here.
experiments/

# Python bytecode + compiled extension built into the source tree (maturin develop)
__pycache__/
*.py[cod]
*.so
*.pyd
